const React = require('react');
const ReactDOM = require('react-dom');

function RootContainer () {
  return (
    <div className="root" />
  );
}

module.exports = function redLotusDomRender (App, rootFilePath) {
  ReactDOM.render();
};
